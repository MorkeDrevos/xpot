// components/ClaimTicketSection.tsx
'use client';

// Temporary stub while X login is being rebuilt.
// Keeps imports simple (no Clerk) and doesn't break dashboard.

export default function ClaimTicketSection() {
  return null;
}
